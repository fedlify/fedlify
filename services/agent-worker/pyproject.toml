[project]
name = "fedlify-agent-worker"
version = "0.1.0"
description = "Fedlify NVFLARE kit generation worker."
requires-python = ">=3.11"
dependencies = []

[project.scripts]
fedlify-agent-worker = "fedlify_agent_worker.main:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
